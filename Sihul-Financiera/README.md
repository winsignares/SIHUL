
  # SIHUL (SGA)2.0 (copia)

  This is a code bundle for SIHUL (SGA)2.0 (copia). The original project is available at https://www.figma.com/design/drqX28uxvMcjPUO2BELa31/SIHUL--SGA-2.0--copia-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  